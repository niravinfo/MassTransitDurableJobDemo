info:
  name: CancelJob
  type: http
  seq: 5
  tags:
    - MassTransitDurableJobDemo

http:
  method: DELETE
  url: "{{baseUrl}}/api/reports/:jobId"
  params:
    - name: jobId
      value: "{{JobId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/reports/:jobId"
      method: DELETE
      params:
        - name: jobId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
