info:
  name: ListEndpoints
  type: http
  seq: 7
  tags:
    - MassTransitDurableJobDemo

http:
  method: GET
  url: "{{baseUrl}}/api/reports"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/reports"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
