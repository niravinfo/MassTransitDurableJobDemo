info:
  name: RetryJob
  type: http
  seq: 6
  tags:
    - MassTransitDurableJobDemo

http:
  method: POST
  url: "{{baseUrl}}/api/reports/:jobId/retry"
  params:
    - name: jobId
      value: "{{JobId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/reports/:jobId/retry"
      method: POST
      params:
        - name: jobId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
